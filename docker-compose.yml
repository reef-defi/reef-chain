version: "3.2"

services:
  dev:
    container_name: reef-node
    image: paritytech/ci-linux:974ba3ac-20201006
    working_dir: /var/www/reef-node
    ports:
      - "9944:9944"
    environment:
      - CARGO_HOME=/var/www/reef-node/.cargo
    volumes:
      - .:/var/www/reef-node
      - type: bind
        source: ./.local
        target: /root/.local
    command: bash -c "cargo build --release && ./target/release/reef-node --dev --ws-external"
